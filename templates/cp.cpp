#include <bits/stdc++.h>
using namespace std;

#ifdef STARCEA
#include "debug.h"
#else
#define debug(...)
#endif

using i64 = int64_t;

constexpr int _PREC = 0;

// #define int i64

void solve() {
    // code here
}

signed main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    cout << fixed << setprecision(_PREC);

    int t;
    cin >> t;

    while (t--) solve();
}
