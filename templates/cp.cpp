#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include "debug.h"
#else
#define debug(...)
#endif

using i64 = int64_t;

constexpr int _PREC = 0;

// #define int i64

void solve() {
    // code here
}

signed main() {
    cin.tie(0)->sync_with_stdio(0);
    cout << fixed << setprecision(_PREC);

    int t;
    cin >> t;

    while (t--) solve();
}
